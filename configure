#!/bin/sh

#
# This script will attempt to build a development tree
# for [rperf] by creating the Makefiles in each directory.
#

rootdir=`pwd`
find . -name Makefile -exec sed -i -e 's|^ROOTDIR[^=]*= .*|ROOTDIR = $rootdir|' {} \;
