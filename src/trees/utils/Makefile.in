# The name of the games
LIBNAME   = rtree-utils
PROGRAM   =

# C source files
LIBSRCS  += rnodes.c
LIBSRCS  += fake-implementation.c
MAINSRCS +=

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RLIBCDIR}
INCLUDE  += -I../include
CFLAGS   +=
C++FLAGS +=
USRLIBS  +=
SYSLIBS  +=
