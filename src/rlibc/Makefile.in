# The name of the games
LIB       = rlibc
PROGRAMS  =

# C source files
LIBSRCS  += vargv.c
LIBSRCS  += sargv.c
LIBSRCS  += plugins.c
LIBSRCS  += rwall.c
LIBSRCS  += safe.c
LIBSRCS  += support.c
LIBSRCS  += rctype.c
LIBSRCS  += roptions.c
LIBSRCS  += other.c
LIBSRCS  += rstring.c

# Compiler flags and System libraries (optional)
INCLUDE   =
CFLAGS    =
C++FLAGS  =
SYSLIBS   =
