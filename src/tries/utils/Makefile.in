# The name of the games
LIBNAME   = rtrie-utils
PROGRAM   =

# C source files
LIBSRCS  += nodes.c
LIBSRCS  += fake-implementation.c
MAINSRCS +=

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RLIBCDIR}
CFLAGS   +=
C++FLAGS +=
USRLIBS  +=
SYSLIBS  +=
