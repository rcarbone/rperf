# The name of the games
LIB       = rtrie-ut
PROGRAM   = run

# C source files
LIBSRCS  += builtins.c
MAINSRCS += main.c

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RLIBCDIR}
INCLUDE  += -I${RUTDIR}
INCLUDE  += -I${UTILSDIR}
CFLAGS   +=
C++FLAGS +=
USRLIBS  += ${RUTLIB}
USRLIBS  += ${UTILSLIB}
SYSLIBS  +=
