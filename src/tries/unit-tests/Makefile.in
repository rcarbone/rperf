# The name of the games
LIBNAME   = rtrie-ut

# C source files
LIBSRCS  += builtins.c
MAINSRCS += run-it.c

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RLIBCDIR}
INCLUDE  += -I${RUTDIR}
INCLUDE  += -I${UTILSDIR}
CFLAGS   +=
C++FLAGS +=
USRLIBS  += ${STLIB}
USRLIBS  += ${RUTLIB}
USRLIBS  += ${UTILSLIB}
USRLIBS  += ${RLIBCLIB}
SYSLIBS  +=
