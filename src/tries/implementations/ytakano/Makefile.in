# The name of the games
NAME       = ytakano

# C/C++ source files
LIBCSRCS   =
LIBC++SRCS = glue.cpp

# Compiler flags and System libraries (optional)
INCLUDE   += -I./pub
INCLUDE   += -I${RUTDIR}
INCLUDE   += -I${UTILSDIR}
INCLUDE   += -I${SUITEDIR}
USRLIBS   += ${SUITELIB}
CFLAGS    +=
C++FLAGS  +=
USRLIBS   += ${SUITELIB}
SYSLIBS   += -lstdc++
