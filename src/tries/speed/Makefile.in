# The name of the games
LIBNAME   = rtrie-speed

# C source files
LIBSRCS  += run.c
LIBSRCS  += sw.c
LIBSRCS  += print.c
MAINSRCS += main.c

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RLIBCDIR}
INCLUDE  += -I${RUTDIR}
INCLUDE  += -I${UTILSDIR}
INCLUDE  += -I${SUITEDIR}
CFLAGS   +=
C++FLAGS +=
USRLIBS  += ${STLIB}
USRLIBS  += ${RUTLIB}
USRLIBS  += ${SUITELIB}
USRLIBS  += ${UTILSLIB}
USRLIBS  += ${RLIBCLIB}
SYSLIBS  += -ldl
