# The name of the games
LIB       = rtrie-speed
PROGRAM   = rtrie-speed

# C source files
LIBSRCS  += run.c
LIBSRCS  += sw.c
LIBSRCS  += print.c
MAINSRCS += main.c

# Compiler flags and System libraries (optional)
INCLUDE  += -I${RUTDIR}
INCLUDE  += -I${UTILSDIR}
INCLUDE  += -I${SUITEDIR}
CFLAGS   +=
C++FLAGS +=
USRLIBS  += ${RUTLIB}
USRLIBS  += ${SUITELIB}
USRLIBS  += ${UTILSLIB}
USRLIBS  += ${RLIBCLIB}
SYSLIBS  += -ldl
