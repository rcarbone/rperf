# The name of the games
LIB       = rht
PROGRAMS  = ut-run suite-run

# C source files
MAINSRCS += ut-run.c
MAINSRCS += gnu-search.c
LIBSRCS  += version.c
LIBSRCS  += datasets.c
LIBSRCS  += elapsed.c
LIBSRCS  += unit.c
LIBSRCS  += unit-code.c
LIBSRCS  += suite.c
LIBSRCS  += suite-code.c

# Compiler flags and System libraries (optional)
INCLUDE   =
CFLAGS    =
C++FLAGS  =
SYSLIBS   =
