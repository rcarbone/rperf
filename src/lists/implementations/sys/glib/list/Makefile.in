# The name of the games
NAME       = ghash

# C/C++ source files
LIBCSRCS   = glue.c
LIBC++SRCS =

# Compiler flags and System libraries (optional)
INCLUDE    = `pkg-config --cflags glib-2.0`
CFLAGS     =
C++FLAGS   =
SYSLIBS    = `pkg-config --libs glib-2.0`
