# The name of the games
LIB       = rl
PROGRAMS  = ut-run suite-run

# C source files
LIBSRCS  += elems.c
LIBSRCS  += elapsed.c
LIBSRCS  += unit.c
LIBSRCS  += unit-code.c
LIBSRCS  += suite.c
LIBSRCS  += suite-code.c
MAINSRCS += ut-run.c
MAINSRCS += bsd-tailq.c

# Compiler flags and System libraries (optional)
INCLUDE   =
CFLAGS    =
C++FLAGS  =
SYSLIBS   =
