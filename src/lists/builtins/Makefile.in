# The name of the games
LIB       = rl
PROGRAMS  = rltest

# C source files
MAINSRCS += main.c
MAINSRCS += bsd-slist.c
LIBSRCS  += elems.c
LIBSRCS  += unit.c
LIBSRCS  += unit-code.c
LIBSRCS  += print.c

# Compiler flags and System libraries (optional)
INCLUDE   =
CFLAGS    =
C++FLAGS  =
SYSLIBS   =
